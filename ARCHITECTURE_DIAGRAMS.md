# 🎨 Application Flow & Architecture Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                         Browser                              │
│  ┌───────────────────────────────────────────────────────┐ │
│  │            React Frontend (Port 3000)                  │ │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │ │
│  │  │  Dashboard  │  │   Tasks     │  │  Employees  │  │ │
│  │  │  Component  │  │  Component  │  │  Component  │  │ │
│  │  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  │ │
│  │         │                 │                 │          │ │
│  │         └─────────────────┼─────────────────┘          │ │
│  │                           │                             │ │
│  │                  ┌────────▼────────┐                   │ │
│  │                  │  API Service    │                   │ │
│  │                  │  (api.js)       │                   │ │
│  │                  └────────┬────────┘                   │ │
│  └───────────────────────────┼──────────────────────────┘ │
└────────────────────────────┼─────────────────────────────┘
                              │
                              │ HTTP/REST
                              │
┌─────────────────────────────▼─────────────────────────────┐
│              Express Backend (Port 5000)                   │
│  ┌────────────────────────────────────────────────────┐   │
│  │                   API Endpoints                     │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────────┐    │   │
│  │  │ Employee │  │   Task   │  │  Dashboard   │    │   │
│  │  │  Routes  │  │  Routes  │  │    Route     │    │   │
│  │  └────┬─────┘  └────┬─────┘  └──────┬───────┘    │   │
│  │       │             │                │             │   │
│  │       └─────────────┼────────────────┘             │   │
│  │                     │                               │   │
│  │           ┌─────────▼─────────┐                    │   │
│  │           │   Database Layer  │                    │   │
│  │           │   (database.js)   │                    │   │
│  │           └─────────┬─────────┘                    │   │
│  └─────────────────────┼──────────────────────────────┘   │
└────────────────────────┼──────────────────────────────────┘
                         │
                         │ SQL Queries
                         │
┌────────────────────────▼──────────────────────────────────┐
│                  SQLite Database                          │
│  ┌──────────────────┐       ┌──────────────────┐         │
│  │  employees       │       │     tasks        │         │
│  │  ┌────────────┐  │       │  ┌────────────┐ │         │
│  │  │ id         │  │       │  │ id         │ │         │
│  │  │ name       │  │       │  │ title      │ │         │
│  │  │ email      │  │       │  │ status     │ │         │
│  │  │ department │  │       │  │ priority   │ │         │
│  │  │ position   │  │       │  │ employee_id│ │         │
│  │  └────────────┘  │       │  └────────────┘ │         │
│  └──────────────────┘       └──────────────────┘         │
│           ▲                          │                     │
│           └──────────────────────────┘                     │
│              Foreign Key Relationship                      │
└───────────────────────────────────────────────────────────┘
```

## User Flow Diagrams

### 1. View Dashboard Flow
```
User Opens App
      │
      ▼
Load Dashboard Component
      │
      ▼
Fetch Dashboard Stats
(GET /api/dashboard)
      │
      ▼
Database Aggregation
(COUNT, SUM calculations)
      │
      ▼
Return Statistics JSON
      │
      ▼
Display 7 Stat Cards
+ Employee Task Table
```

### 2. Create Task Flow
```
User Clicks "Add Task"
      │
      ▼
Open Task Modal
      │
      ▼
Fetch Employees List
(GET /api/employees)
      │
      ▼
Populate Employee Dropdown
      │
      ▼
User Fills Form
      │
      ▼
Submit Form
(POST /api/tasks)
      │
      ▼
Validate Input
      │
      ├─ Invalid → Show Error
      │
      ▼ Valid
Insert into Database
      │
      ▼
Return New Task
      │
      ▼
Close Modal
      │
      ▼
Refresh Task List
```

### 3. Filter Tasks Flow
```
User Selects Filter
(Status/Employee/Priority)
      │
      ▼
Update Filter State
      │
      ▼
Build Query Parameters
(?status=pending&priority=high)
      │
      ▼
Fetch Filtered Tasks
(GET /api/tasks?filters)
      │
      ▼
Database WHERE Clause
      │
      ▼
Return Filtered Results
      │
      ▼
Update Task Table Display
```

### 4. Update Task Status Flow
```
User Clicks "Edit Task"
      │
      ▼
Open Task Modal
(Pre-filled with data)
      │
      ▼
User Changes Status
(pending → in-progress)
      │
      ▼
Submit Update
(PUT /api/tasks/:id)
      │
      ▼
Validate Input
      │
      ▼
Update Database
(SET status, updated_at)
      │
      ▼
Return Updated Task
      │
      ▼
Close Modal
      │
      ▼
Refresh Task List
      │
      ▼
Dashboard Stats Auto-Update
```

### 5. Delete Employee Flow
```
User Clicks "Delete"
      │
      ▼
Show Confirmation Dialog
"Delete employee and all tasks?"
      │
      ├─ Cancel → Stop
      │
      ▼ Confirm
Send Delete Request
(DELETE /api/employees/:id)
      │
      ▼
Database CASCADE Delete
(Employee + All Tasks)
      │
      ▼
Return Success
      │
      ▼
Refresh Employee List
      │
      ▼
Refresh Dashboard
(Task counts updated)
```

## Component Hierarchy

```
App.jsx
├── Header
│   ├── Title
│   └── Navigation
│       ├── Dashboard Button
│       ├── Tasks Button
│       └── Employees Button
│
└── Main
    ├── Dashboard.jsx
    │   ├── Stats Grid
    │   │   ├── Total Tasks Card
    │   │   ├── Completed Card
    │   │   ├── In Progress Card
    │   │   ├── Pending Card
    │   │   ├── Employees Card
    │   │   ├── Completion Rate Card
    │   │   └── High Priority Card
    │   │
    │   └── Employee Tasks Table
    │       └── Task Rows
    │
    ├── TaskList.jsx
    │   ├── Header
    │   │   └── Add Task Button
    │   ├── Filters
    │   │   ├── Status Filter
    │   │   ├── Employee Filter
    │   │   ├── Priority Filter
    │   │   └── Clear Button
    │   ├── Task Table
    │   │   └── Task Rows
    │   │       ├── Task Data
    │   │       ├── Status Badge
    │   │       ├── Priority Badge
    │   │       └── Action Buttons
    │   └── TaskModal.jsx (conditional)
    │       ├── Form Fields
    │       ├── Cancel Button
    │       └── Submit Button
    │
    └── EmployeeList.jsx
        ├── Header
        │   └── Add Employee Button
        ├── Employee Table
        │   └── Employee Rows
        │       ├── Employee Data
        │       └── Action Buttons
        └── EmployeeModal.jsx (conditional)
            ├── Form Fields
            ├── Cancel Button
            └── Submit Button
```

## API Request/Response Flow

### Example: Create New Task

```
Frontend                    Backend                    Database
   │                          │                           │
   │  POST /api/tasks         │                           │
   │  {                       │                           │
   │    title: "Fix Bug",     │                           │
   │    status: "pending",    │                           │
   │    priority: "high",     │                           │
   │    employee_id: 1        │                           │
   │  }                       │                           │
   ├─────────────────────────>│                           │
   │                          │                           │
   │                          │  Validate Input           │
   │                          │                           │
   │                          │  INSERT INTO tasks        │
   │                          │  VALUES (...)             │
   │                          ├──────────────────────────>│
   │                          │                           │
   │                          │                           │
   │                          │  New Task ID: 11          │
   │                          │<──────────────────────────┤
   │                          │                           │
   │                          │  SELECT task with         │
   │                          │  employee name            │
   │                          ├──────────────────────────>│
   │                          │                           │
   │                          │  Task + Employee Data     │
   │                          │<──────────────────────────┤
   │                          │                           │
   │  201 Created             │                           │
   │  {                       │                           │
   │    id: 11,               │                           │
   │    title: "Fix Bug",     │                           │
   │    employee_name: "John",│                           │
   │    ...                   │                           │
   │  }                       │                           │
   │<─────────────────────────┤                           │
   │                          │                           │
   │  Update UI               │                           │
   │  Close Modal             │                           │
   │  Refresh List            │                           │
   │                          │                           │
```

## Data Flow State Management

```
┌──────────────────────────────────────────────────┐
│              React Component State               │
├──────────────────────────────────────────────────┤
│                                                  │
│  const [tasks, setTasks] = useState([])         │
│  const [loading, setLoading] = useState(true)   │
│  const [error, setError] = useState(null)       │
│  const [showModal, setShowModal] = useState()   │
│                                                  │
│  useEffect(() => {                              │
│    fetchTasks() ────> API Call                  │
│  }, [filters])     │                            │
│                    │                             │
│                    ▼                             │
│              Update State                        │
│              setTasks(data)                      │
│              setLoading(false)                   │
│                    │                             │
│                    ▼                             │
│              Component Re-render                 │
│              Display Updated Data                │
│                                                  │
└──────────────────────────────────────────────────┘
```

## Database Relationship Visualization

```
┌─────────────────────────────────────────────────────┐
│                    employees                        │
├──────────────┬──────────────────────────────────────┤
│ id (PK)      │ Primary Key, Auto Increment          │
│ name         │ NOT NULL                             │
│ email        │ UNIQUE, NOT NULL                     │
│ department   │ NOT NULL                             │
│ position     │ NOT NULL                             │
│ created_at   │ DEFAULT CURRENT_TIMESTAMP            │
└──────────────┴──────────────────────────────────────┘
       │
       │ 1 employee has many tasks
       │
       │ ON DELETE CASCADE
       │ (Delete employee → Delete all their tasks)
       │
       ▼
┌─────────────────────────────────────────────────────┐
│                      tasks                          │
├──────────────┬──────────────────────────────────────┤
│ id (PK)      │ Primary Key, Auto Increment          │
│ title        │ NOT NULL                             │
│ description  │ Optional                             │
│ status       │ CHECK (pending|in-progress|completed)│
│ priority     │ CHECK (low|medium|high)              │
│ employee_id  │ FOREIGN KEY → employees.id           │
│ due_date     │ Optional                             │
│ created_at   │ DEFAULT CURRENT_TIMESTAMP            │
│ updated_at   │ DEFAULT CURRENT_TIMESTAMP            │
└──────────────┴──────────────────────────────────────┘
```

## Responsive Layout Breakpoints

```
Desktop (> 768px)
┌────────────────────────────────────────┐
│  Header - Full Width                   │
├────────────────────────────────────────┤
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ │
│  │ Card │ │ Card │ │ Card │ │ Card │ │
│  └──────┘ └──────┘ └──────┘ └──────┘ │
│  ┌──────────────────────────────────┐ │
│  │        Full Table Display        │ │
│  │  All columns visible             │ │
│  └──────────────────────────────────┘ │
└────────────────────────────────────────┘

Tablet (480-768px)
┌──────────────────────┐
│  Header - Wrapped    │
├──────────────────────┤
│  ┌──────┐ ┌──────┐  │
│  │ Card │ │ Card │  │
│  └──────┘ └──────┘  │
│  ┌──────┐ ┌──────┐  │
│  │ Card │ │ Card │  │
│  └──────┘ └──────┘  │
│  ┌────────────────┐ │
│  │ Table Scroll → │ │
│  └────────────────┘ │
└──────────────────────┘

Mobile (< 480px)
┌───────────────┐
│  Header       │
│  Nav Stacked  │
├───────────────┤
│  ┌─────────┐ │
│  │  Card   │ │
│  └─────────┘ │
│  ┌─────────┐ │
│  │  Card   │ │
│  └─────────┘ │
│  ┌─────────┐ │
│  │  Card   │ │
│  └─────────┘ │
│  ┌─────────┐ │
│  │ Table → │ │
│  │ Scroll  │ │
│  └─────────┘ │
└───────────────┘
```

## Technology Stack Visual

```
┌─────────────────────────────────────────────────┐
│                   Frontend                      │
├─────────────────────────────────────────────────┤
│  React 18.2  │  Component-based UI Library     │
│  Vite 5.0    │  Fast dev server & bundler      │
│  CSS3        │  Responsive styling             │
│  Fetch API   │  HTTP client                    │
└─────────────────────────────────────────────────┘
                      ▲ │
                      │ │ HTTP/JSON
                      │ ▼
┌─────────────────────────────────────────────────┐
│                    Backend                      │
├─────────────────────────────────────────────────┤
│  Node.js 18+ │  JavaScript runtime             │
│  Express 4.x │  Web framework                  │
│  CORS        │  Cross-origin support           │
│  dotenv      │  Environment config             │
└─────────────────────────────────────────────────┘
                      ▲ │
                      │ │ SQL
                      │ ▼
┌─────────────────────────────────────────────────┐
│                   Database                      │
├─────────────────────────────────────────────────┤
│  SQLite3     │  Embedded SQL database          │
│  2 Tables    │  employees, tasks               │
│  Constraints │  Foreign keys, CHECK, UNIQUE    │
└─────────────────────────────────────────────────┘
```

---

This visual guide helps understand:
- ✅ System architecture
- ✅ Component relationships
- ✅ Data flow
- ✅ User interactions
- ✅ Database structure
- ✅ API communication
- ✅ Responsive design
- ✅ Technology stack

Perfect for presentations and documentation! 🎯
